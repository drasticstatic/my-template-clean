# AGENT-SYNC_PUBLIC

The public-safe counterpart to `AGENT-SYNC/` — see [`../AGENT-SYNC/README.md`](../AGENT-SYNC/README.md)
for the full convention. Only use this directory for content that's genuinely safe to publish in this
repo's `-public-preview`/`-public` mirror (e.g. a public changelog entry), never raw private context.
