# created-by-Littlebird

> **This is the canonical Pattern B version.** Repos with no private lane (public-only, using
> `AGENT-SYNC_PUBLIC/` as their single coordination directory — see `AGENT-SYNC/README.md` for the
> full Pattern A/B explanation) should copy this file's structure, then replace "My role in
> my-template" below with a repo-specific section grounded in *that* repo's own README.

> **Disclaimer:** Everything in this directory is generated by LittlebirdAI — an AI assistant that observes
> Christopher's screen, calendars, meetings, and chat history. Accuracy is not guaranteed. These are
> *coordination notes*, not canonical artifacts. **Consult Christopher before acting on anything here.**

## Welcome, fleet! 🐦

Hey Alfred, Fortuna, Kavanah, Mystarch, Auggie, and Christopher!

I'm Littlebird — the agent that *observes*. I watch Christopher's screen, track his 9+ calendars,
transcribe meetings, and remember everything so the rest of you don't have to start from zero.

## My role in my-template

This is the Pattern B template placeholder — `my-template` itself doesn't need a Pattern B lane (it
has both `AGENT-SYNC/` and `AGENT-SYNC_PUBLIC/` as reference structures, not as an active repo of its
own). When a new public-only repo copies this file, replace this section with what Littlebird
actually does there, grounded in that repo's real README — see `resume` or
`gratitude-token-project_docs` for worked examples.

## Agent-specific handoffs in this repo

None — this is a template placeholder, not an active handoff lane.

---

*Established Sep 9, 2026 — added after the fleet audit found this directory missing here even though
every Pattern B repo (`resume`, `gratitude-token-project_docs`) already had one, meaning new Pattern B
repos had nothing to copy from. This directory in `my-template` is the canonical Pattern B reference.*
